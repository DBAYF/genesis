# =============================================================================
# GENESIS ENGINE - DOCKER COMPOSE OVERRIDE (DEVELOPMENT)
# =============================================================================
# This file contains development-specific overrides
# It is automatically merged with docker-compose.yml

version: '3.8'

services:
  # =============================================================================
  # DEVELOPMENT OVERRIDES
  # =============================================================================

  api-gateway:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/api-gateway/src:/app/src
      - ./backend/api-gateway/package.json:/app/package.json
    command: npm run dev

  auth-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/auth-service/src:/app/src
      - ./backend/auth-service/package.json:/app/package.json
    command: npm run dev

  user-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/user-service/src:/app/src
      - ./backend/user-service/package.json:/app/package.json
    command: npm run dev

  company-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/company-service/src:/app/src
      - ./backend/company-service/package.json:/app/package.json
    command: npm run dev

  financial-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/financial-service/src:/app/src
      - ./backend/financial-service/package.json:/app/package.json
    command: npm run dev

  compliance-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/compliance-service/src:/app/src
      - ./backend/compliance-service/package.json:/app/package.json
    command: npm run dev

  crm-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/crm-service/src:/app/src
      - ./backend/crm-service/package.json:/app/package.json
    command: npm run dev

  calendar-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/calendar-service/src:/app/src
      - ./backend/calendar-service/package.json:/app/package.json
    command: npm run dev

  billing-service:
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    volumes:
      - ./backend/billing-service/src:/app/src
      - ./backend/billing-service/package.json:/app/package.json
    command: npm run dev

  frontend:
    environment:
      - NODE_ENV=development
    volumes:
      - ./app:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev